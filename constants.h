// constants.h
#pragma once

constexpr float BOARD_SIZE = 1000.f;
constexpr float WINDOW_WIDTH = 1480.f;
constexpr float WINDOW_HEIGHT = 1100.f;
constexpr float OFFSET = (WINDOW_WIDTH - BOARD_SIZE) / 2.f;
constexpr float BORDER_WIDTH = 50.f; // si besoin (pour View)
